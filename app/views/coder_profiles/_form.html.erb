<%= form_for @coder_profile, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> /Coder Profile</legend>

    <div class="control-group">
      <%= f.label :github_profile, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :github_profile, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', coder_profiles_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
