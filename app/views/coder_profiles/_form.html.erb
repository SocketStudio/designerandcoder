<%= form_for(@coder_profile) do |f| %>
  <% if @coder_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coder_profile.errors.count, "error") %> prohibited this coder_profile from being saved:</h2>

      <ul>
      <% @coder_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :github_profile %><br />
    <%= f.text_field :github_profile %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
